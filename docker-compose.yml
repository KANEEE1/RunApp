version: "3.9"

services:
  # api:
  #   build: .
  #   container_name: runtrack_api
  #   restart: always
  #   env_file: .env
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     - db
  #   command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  db:
    image: postgres:15
    container_name: runtrack_db
    restart: always
    environment:
      POSTGRES_USER: kane1
      POSTGRES_PASSWORD: cringe123
      POSTGRES_DB: runapp_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
